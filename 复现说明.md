# 实验结果复现说明

## 1 环境要求

本实验只需要 CPU。数据来自 scikit-learn 内置的 `load_digits`，运行时不会下载数据。

本次报告使用的环境为：

- Windows 10.0.26200；
- Python 3.10.20；
- PyTorch 2.5.1；
- scikit-learn 1.7.2；
- NumPy 2.0.1；
- Matplotlib 3.10.9。

其他满足 Python 3.10 及以上、PyTorch 2.0 及以上的兼容环境也可以运行。请在开始前
确认当前解释器已经包含 `torch`、`sklearn`、`numpy` 和 `matplotlib`。仓库本身不会
自动创建环境或安装依赖。

可以用下面的命令检查当前解释器：

```powershell
python -c "import sklearn; import torch, numpy, matplotlib; print('Python/PyTorch环境可用'); print('PyTorch:', torch.__version__); print('CUDA可用:', torch.cuda.is_available())"
```

本实验应显示 `CUDA可用: False` 或在不使用CUDA的情况下直接通过CPU运行。

## 2 固定实验协议

为保证对照公平，脚本固定以下设置：

- 数据集：`load_digits`，共1797个样本、64维输入、10个类别；
- 预处理：像素值除以16，归一化到 `[0, 1]`；
- 数据划分：训练集80%、测试集20%，分层抽样；
- 数据划分随机种子：42；
- 基线训练：全批量、30个epoch、交叉熵损失；
- 普通实验初始化种子：42；
- 最优组合复测种子：42、43、44。

除最优组合和学习率扫描外，每个对照组都只按照实验设计修改指定变量。

## 3 运行全部实验

在仓库根目录运行：

```powershell
python .\mlp_digits.py --experiment all
```

如果在本实验原始电脑上运行，为避免终端中的其他Python覆盖Conda环境，可以显式
指定已有解释器：

```powershell
& '<已有环境的 Python 路径>' .\mlp_digits.py --experiment all
```

该命令依次运行基线、实验1至实验7、失败实验和最优组合三次复测。

## 4 单独复现实验

命令中的实验名称可以替换为：

- `baseline`：基线；
- `exp1` 至 `exp7`：七组单变量对照；
- `failure`：学习率10.0的失败实验；
- `best`：种子42、43、44的最优组合复测；
- `lr_scan`：扫描学习率0.1、1.0、5.0、10.0和20.0。

例如，仅复现学习率扫描：

```powershell
python .\mlp_digits.py --experiment lr_scan
```

## 5 输出文件

运行后会在仓库根目录生成：

- `results.csv`：各组最终准确率、耗时和种子；
- `best_runs_results.csv`：最优组合三次复测明细；
- `lr_scan_results.csv`：学习率扫描汇总；
- `history_*.csv`：每组逐epoch损失和准确率；
- `result_*.png`：各组曲线、汇总图和学习率扫描图。

这些文件均可由脚本重新生成，因此默认不提交到Git。

## 6 报告参考结果

固定种子下，本次实验得到以下准确率：

| 组号 | 主要改动 | 测试准确率 |
|---:|---|---:|
| 0 | 基线：1×32 Sigmoid + SGD(0.1) | 22.22% |
| 1 | Sigmoid改为ReLU | 47.50% |
| 2 | 加深为2×128 Sigmoid | 7.22% |
| 3 | 改为Adam(0.01) | 82.22% |
| 4 | SGD学习率改为0.01 | 10.00% |
| 5 | L2正则化 `weight_decay=1e-4` | 22.22% |
| 6 | Dropout `p=0.2` | 20.00% |
| 7 | 加入BatchNorm | 79.17% |
| 8 | 最优组合三次平均 | 95.65% ± 1.25% |
| 失败实验 | 基线学习率改为10.0 | 36.94% |

最优组合三次准确率分别为97.22%、94.17%和95.56%。学习率扫描中，`lr=5.0`
开始出现明显震荡，稳定与不稳定的临界区间位于1.0至5.0之间，`lr>=10.0`无法
稳定收敛。

训练耗时受CPU负载、首次运行初始化和线程调度影响，复现时不要求与报告中的秒数
完全一致；准确率和曲线趋势才是主要核对项。

## 7 复现检查

运行完成后应确认：

1. `results.csv` 包含基线、七组改进、失败实验和最优组合；
2. 最优组合生成三张曲线图 `result_exp8_run1.png` 至 `result_exp8_run3.png`；
3. `result_lr_too_big.png` 呈现明显震荡；
4. `result_lr_scan.png` 能比较五个学习率的损失和准确率；
5. 所有图片中文标题、图例和坐标轴正常显示。
